<section class="hero">
  <div class="hero-content">
    <h1>Verificación y supervisión producto y  <span> activación garantía oficial</span></h1>
    <p>Servicio de posventa para compras <br> en tiendas en España.</p>
    <a class="primary-button" href="#servicios">Selecciona tipología de servicio</a>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    min-height: 100dvh;
    background-image:
      linear-gradient(90deg, rgba(255, 244, 222, 0.9), rgba(255, 244, 222, 0.1) 58%),
      url("/images/hero-revo.jpg");
    background-size: cover;
    background-position: center top;
    display: flex;
    align-items: center;
  }

  .hero-content {
    padding-left: clamp(24px, 3.8vw, 47px);
    padding-top: 70px;
  }

  h1 {
    margin: 0;
    font-size: clamp(42px, 4.1vw, 57px);
    line-height: 1.12;
    font-weight: 900;
    animation: fadeUp 800ms ease both;
  }

  h1 span {
    display: block;
    color: var(--color-orange);
  }

  p {
    margin: 40px 0 0;
    font-size: clamp(20px, 1.65vw, 25px);
    line-height: 1.2;
    font-weight: 800;
    animation: fadeUp 800ms ease 140ms both;
  }

  .primary-button {
    margin-top: 55px;
    display: inline-flex;
    min-height: 38px;
    padding: 0 18px;
    align-items: center;
    border-radius: 5px;
    color: #fff;
    background: var(--color-orange);
    font-size: 18px;
    text-decoration: none;
    animation: fadeUp 800ms ease 260ms both;
  }

  @media (max-width: 768px) {
    .hero {
      background-image:
        linear-gradient(180deg, rgba(255, 244, 222, 0.92), rgba(255, 244, 222, 0.6) 100%),
        url("/images/hero-revo.jpg");
      align-items: flex-end;
    }

    .hero-content {
      max-width: 100%;
      padding: 0 clamp(24px, 5vw, 40px) 60px;
      padding-top: 0;
    }
  }
</style>